apiVersion: v1
kind: Pod
metadata:
  name: svc-test
  namespace: roboshop
spec:
  containers:
  - name: almalinux
    image: almalinux
    command: ["/bin/sleep", "1000"]
    ports:
    - containerPort: 80